<div class="page-title">
  <h3>部下一覧</h3>
</div>
<div class="table-title center-align">
  <%= current_user.name%> さんの部下一覧
</div>
  <table class="bordered highlight">
    <tbody>
      <% @subordinates.each do |subordinate| %>
        <tr>
          <td><%= subordinate.name %></td>
          <td><%= subordinate.email %></td>
          <td><%= link_to '評価する', "/answers/new/#{subordinate.id}" %></td>
        <% if subordinate.answered_answer.present? %>
          <td><%= link_to '評価閲覧', user_path(subordinate.id) %></td>
        <% elsif %>
          <td></td>
        <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
  <br>
